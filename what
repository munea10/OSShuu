def solution(n):
   
  
    hap = 0
    msg = str(n)
    l = list(msg)

    for c in l:
        hap += int(c)


    # 나머지 0?
    if n % hap == 0:
        return True
    else:
        return False

arr = [10, 12, 11, 13]
for n in arr:
    print(solution(n))
